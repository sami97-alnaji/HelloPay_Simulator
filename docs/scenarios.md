# Scenarios

Scenario Studio provides approval, rejection, cancellation, terminal-busy,
timeout, session-expired, permission, amount, tip, refund, void, recovery,
app-termination, and custom JSON simulator outcomes. Its state transitions and
delays affect the shared engine and are not production processor behavior.
